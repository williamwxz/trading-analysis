# Project Context: Trading Analysis

## Infrastructure & Deployment

- **Primary Region**: `ap-northeast-1` (Tokyo)
- **Native AWS CI/CD**:
  - **Source**: GitHub (via AWS CodeStar Connections)
  - **Connection ARN**: `arn:aws:codeconnections:ap-northeast-1:339163283253:connection/ef9b3b42-c3f2-4a90-98a0-2c4a2869ade2`
  - **Pipeline**: AWS CodePipeline (`trading-analysis`)
  - **Compute**: AWS CodeBuild (Native)
  - **Environment**: Single environment (simplified naming)

## Architecture

| Component | AWS Resource | Role |
| --- | --- | --- |
| **Access** | **ALB** | Stable URL for Dagster (`/`) and Grafana (`/grafana`) |
| **Orchestrator** | ECS Fargate | Runs Dagster Webserver & Daemon |
| **Metadata DB** | RDS Postgres | Stores Dagster run history and logs |
| **Analytics DB** | ClickHouse Cloud | External managed service for trade data |
| **Stream Buffer** | MSK Serverless | Kafka interface for real-time ingestion |
| **Data Lake** | S3 | Raw data storage (`trading-analysis-data-v2`) |
| **Visualization**| ECS Fargate | Runs Grafana for PnL dashboards |

## Monthly Cost Estimation (Tokyo)

| Service | Estimated Cost | Notes |
| --- | --- | --- |
| **MSK Serverless** | ~$540.00 | **High Cost**: $0.75/hr base charge. |
| **ALB** | ~$16.00 | Application Load Balancer. |
| **RDS Postgres** | ~$20.00 | `db.t4g.micro` (Dagster metadata). |
| **ECS Fargate** | ~$18.00 | Using **Fargate Spot** for cost saving. |
| **Other (S3/Logs/Secrets)** | ~$10.00 | Usage dependent. |
| **Total** | **~$604.00** | |

## Deployment Workflow

1.  **CodePush**: Push to GitHub `main` branch.
2.  **CodePipeline**: Automatically triggers in Tokyo.
3.  **Test Stage**: Runs `pytest`.
4.  **Build Stage**: Docker build (via ECR Public) and push to local ECR.
5.  **Deploy Stage**: 
    - Terraform apply (Infrastructure updates, ALB provisioning).
    - ECS Service update (Force new deployment).
    - Database schema sync (ClickHouse).

## Accessing the Platform

The platform is accessible via a single Application Load Balancer URL:
- **Dagster**: `http://<alb-dns-name>/`
- **Grafana**: `http://<alb-dns-name>/grafana` (Login: `admin` / `changeme`)

## Security Note

- **CodeBuild IAM**: The `trading-analysis-codebuild` role is granted specific service management permissions in the Terraform code.
- **RDS Password**: Automatically generated by Terraform and stored in Secrets Manager (`trading-analysis/dagster-pg`).
