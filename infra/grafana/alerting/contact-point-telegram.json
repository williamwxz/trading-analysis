{
  "uid": "telegram-divergence",
  "name": "telegram-divergence",
  "type": "telegram",
  "settings": {
    "bottoken": "${TELEGRAM_BOT_TOKEN}",
    "chatid": "${TELEGRAM_CHAT_ID}",
    "parse_mode": "None",
    "message": "{{ .CommonLabels.alertname }}\n{{ range .Alerts.Firing }}{{ with index .Annotations \"Error\" }}⚠️ evaluation failed — {{ . }}\n{{ else }}🔴 {{ if .Annotations.summary }}{{ .Annotations.summary }}{{ else }}{{ .Labels }}{{ end }}\n{{ end }}{{ end }}{{ range .Alerts.Resolved }}✅ {{ if .Annotations.summary }}{{ .Annotations.summary }}{{ else }}{{ .Labels }}{{ end }}\n{{ end }}"
  },
  "disableResolveMessage": false
}
