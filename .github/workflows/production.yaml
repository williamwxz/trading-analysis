# This file is intentionally disabled.
# CI/CD is handled by AWS CodePipeline (AWS native approach).
# See infra/terraform/codepipeline.tf and buildspec/ for pipeline definition.
# You can delete this file and the .github/ directory.
